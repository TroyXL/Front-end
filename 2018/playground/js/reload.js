(function () {
  function reloadByArgsLength (arg1, arg2, arg3) {
    const length = arguments.length
    if (length === 0) {
      // TODO logic a
    } else if (length === 1) { // arg1 is exsist
      // TODO logic b
    } else if (length === 2) { // arg1 arg2 are exsist
      // TODO logic c
    } else { // all arguments are exsist
      // TODO logic d
    }
  }
}())